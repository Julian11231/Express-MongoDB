<%- include('partials/header'); -%>
    <div class="container px-5 pt-3 pb-5">
        <h2 class="mb-3">
            <a href="/tiendas" class="text-danger"><i style='font-size:24px' class='fas'>&#xf359;</i></a>
            <%= tienda[0].nombreTienda %>
        </h2>
        <table class="table table-hover shadow w-75">
            <thead class="thead-dark">
                <tr>
                    <th>Catálogos</th>
                </tr>
            </thead>
            <tbody>
                <% for(var i=0; i < catalogos.length; i++) { %>
                    <tr>
                        <td>
                            <a href="/articulos/?catalogoArticulo=<%= catalogos[i]._id%>&tienda=<%= tienda[0].nombreTienda %>">
                                <%= catalogos[i].nombreCatalogo%>
                            </a>
                        </td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
    <%- include('partials/footer'); -%>